{
	readset(s);
	readint(i);
	var index = 0;
	var length = (len(s));
	set result = emptyset;
	while(index < length && i>0){
		add(result,"a".find(s,index));
		var index = index + 1;
		var i = i - 1;
	}
	printset(result);
}
