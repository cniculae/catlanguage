catDo^o.o^{
	readset(s);
	readint(i);
	var index = 0;
	var length = (len(s));
	set result = emptyset;
	while(index < length && index < i){
		add(result,"a".find(s,index));
		index = index + 1;
	}
	printset(result);
}catDone^=.=^
